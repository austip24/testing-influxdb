import { Row } from "./components/row";

const App: React.FC = () => {
	return (
		<div className="h-screen bg-slate-900 text-slate-50">
			<Row />
			hello world
		</div>
	);
};

export default App;
